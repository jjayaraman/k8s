apiVersion: v1
kind: Service
metadata:
  name: {{ include "mongo-crud-sb.fullname" . }}
  labels:
    {{- include "mongo-crud-sb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mongo-crud-sb.selectorLabels" . | nindent 4 }}
